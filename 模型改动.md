Temporal-Dynamic Squeeze-and-Excitation (TDSE) Layer.
    
    相较于传统 SE (只关注全局均值 mean)，TDSE 引入了两个时间维度的动态特征，
    旨在捕捉口音识别中关键的"发音动态"和"韵律模式"：

    1. tdiff (Temporal Difference / 瞬时动态):
       - 目的: 捕捉短时的瞬时变化，如辅音的爆破、摩擦或音高的快速转折。
       - 细节: 计算相邻时间帧的差分绝对值 |x_t - x_{t-1}|，并在全图求均值。
               这反映了特征在微观时间尺度上的"变化剧烈程度"。

    2. twin (Temporal Window / 局部起伏):
       - 目的: 捕捉中长时程的能量起伏，如音节、词级别的韵律重音，比瞬时差分更宏观且抗噪。
       - 细节: 使用自适应长度的滑动窗口 (win ≈ W/8)，计算每个窗口的局部均值。
               然后通过 Softmax 对窗口进行加权聚合，使模型更关注能量突出的关键片段
               (Salient Segment)，忽略静音或平稳底噪。

    最终 Gate 输入为: Concat([mean, twin, tdiff]) -> FC -> Sigmoid